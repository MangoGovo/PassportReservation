spring:
  datasource:
    url: jdbc:h2:mem:passport_reservation;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never
  data:
    redis:
      host: localhost
      port: 6379

app:
  security:
    sensitive-key: 0123456789abcdef0123456789abcdef
    query-hash-salt: test
    audit-hmac-key: test-audit-key
